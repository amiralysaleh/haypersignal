name: Keep Render Alive

on:
  schedule:
    - cron: "*/8 * * * *"   # هر ۱۰ دقیقه یک بار
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render App
        run: curl -s https://hypersignal-si7e.onrender.com > /dev/null
